@model IEnumerable<BD107ProjetFinalV2.Models.LigneCommande>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Quantite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prix)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Commande)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Produit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantite)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prix)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Commande.CommandeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Produit.ProduitId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LigneCommandeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LigneCommandeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LigneCommandeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

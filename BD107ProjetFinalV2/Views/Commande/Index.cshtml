@model IEnumerable<BD107ProjetFinalV2.Models.Commande>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DateParution)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LigneCommandeId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EtatCommande)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DateParution)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClientId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LigneCommandeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EtatCommande.Etat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CommandeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CommandeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CommandeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
